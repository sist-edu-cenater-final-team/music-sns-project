<?xml version="1.0" encoding="UTF-8"?>

<!-- ==== mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="eumpyoCharge">

	<!-- 사용자 코인 증가 -->
  	<update id="increaseUserCoin" parameterType="hashmap">
    	update users
       		set coin = nvl(coin, 0) + #{coin}
     	where user_id = #{userId}
  	</update>

  	<!-- 코인 충전내역 기록 -->
  	<insert id="insertChargeHistory" parameterType="hashmap">
    	insert into coin_history (user_id, coin, at_that_price, created_at)
    	values (#{userId}, #{coin}, #{atThatPrice}, systimestamp)
  	</insert>

  	<!-- 사용자 현재 코인 조회 (users.coin) -->
  	<select id="selectUserCoin" parameterType="long" resultType="long">
    	select nvl(u.coin, 0)
      	from users u
     	where u.user_id = #{value}
  	</select>
</mapper>

